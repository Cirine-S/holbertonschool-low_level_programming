#include "holberton.h"
/**
 *_abs - print the absolute value of an integer
 *@n: the integer that will give the absolute
 *Return: 0
 */
int _abs(int n)
{
if (n < 0)
{
n = -n;
return (n);
}
else
return (n);
}
